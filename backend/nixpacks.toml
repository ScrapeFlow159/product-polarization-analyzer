[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "python -m pip install --no-cache-dir fastapi uvicorn pandas numpy scikit-learn pydantic python-jose passlib python-dotenv"
]