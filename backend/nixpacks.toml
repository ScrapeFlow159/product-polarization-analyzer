[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = [
  "pip install --no-cache-dir -r requirements.txt",
  "pip install uvicorn"
]

[phases.start]
cmds = ["python -m uvicorn main:app --host 0.0.0.0 --port $PORT"]